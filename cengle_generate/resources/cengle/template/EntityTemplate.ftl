package ${bussPackage}.model#if($!entityPackage).${entityPackage}#end;

import com.ftpms.base.model.BaseModel;

/**
 * ${codeName}实体类，对应于数据库表${tableName}
 * <br>
 * <b>功能：</b>${className}<br>
 */
public class ${className} extends BaseModel {
	
	${feilds}
}

